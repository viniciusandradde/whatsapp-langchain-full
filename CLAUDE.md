# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project context

Educational, production-ready harness around WhatsApp agents built with LangGraph. The repo's pedagogy is the *harness around the agent*, not the agent itself: reliable inbound, async processing, durable context/memory, retries, rate limits, and operability. README and docs are in Portuguese (pt-BR); keep new docs/comments in pt-BR to match.

## ⛳ Contrato de entrega: feature nova passa pelo DEV antes de produção

**Merge no master auto-deploya em produção** (CI/CD com filtro por caminho) — mergear É deployar. Por decisão do dono (2026-08-25), **toda feature nova é validada no ambiente de desenvolvimento ANTES do merge**:

1. Feature em branch.
2. Subir no dev: `docker compose -p chatnexus-dev -f docker-compose.yml -f docker-compose.override.yml up -d --build` (rebuild obrigatório — restart não pega edits). Painel dev: `https://chatnexus.hospitalevangelico.com.br` (login `admin@dev.local`).
3. Validar no dev: testes dirigidos + fumaça real pela tela/API (UI ganha captura de tela).
4. Mostrar ao dono no dev **antes** do merge.
5. Só então PR → CI verde → merge (= produção). Depois do deploy, conferir o container NOVO (deploy verde ≠ código novo servindo).

Exceção: hotfix de incidente em produção, somente com ordem explícita do dono. Mudança docs-only (CLAUDE.md, docs/) fica fora do filtro de build e pode ir direto.

## Common commands

All Python work goes through `uv` (locked via `uv.lock`). The Makefile is the canonical entry point — prefer `make <target>` over raw commands so behavior matches CI.

Setup and dev:
- `make setup` — `uv venv` + `uv pip install -e ".[dev]"`
- `make up` / `make down` / `make reset` — full Docker stack (db + api + worker + frontend)
- `make db` — Postgres only (used when iterating on `make api`/`make worker`/`make frontend` outside Docker)
- `make migrate` — apply pending SQL migrations from `db/migrations/`
- `make api` — `uvicorn whatsapp_langchain.server.main:app --reload --port 8000`
- `make worker` — `python -m whatsapp_langchain.worker.main`
- `make frontend` — `cd frontend && npm run dev` (Next.js 16 / React 19)
- `make dev` — LangGraph Studio for agent iteration (`uv run langgraph dev`, reads `langgraph.json`)

Quality (these only check style/types, not logic):
- `make check` — `ruff check` + `ruff format --check` + `pyright src/` (run before commit)
- `make fix && make format` — auto-fix + format
- `make ci` — `check` + `pytest -m "not docker_demo"` (what CI runs)

Tests:
- `make test` — full suite minus `docker_demo` marker
- `make test-x` / `make test-v` — fail-fast / verbose
- Single test: `uv run pytest tests/unit/test_queue_claim.py::test_specific -v`
- `make test-live` — live OpenRouter tests; requires real `OPENROUTER_API_KEY` *and* `OPENROUTER_LIVE_TESTS=1` (the conftest fixture skips otherwise; placeholder keys like `sk-or-v1-xxx` are rejected)
- `make test-demo` / `make test-demo-up` — `docker_demo` marker; needs full Docker stack running
- `make test-flows` — realistic flow tests (`tests/integration/test_realistic_flows.py`); needs Docker stack
- pytest is configured `asyncio_mode = "auto"` — async tests don't need `@pytest.mark.asyncio`
- **Rodando a suite local fora de CI, use `-m "not docker_demo"`** — o TestClient trava em máquina local por causa do lookup de metadata da cloud

**E2E pós-feature obrigatório** — toda feature nova (sprint/endpoint/fluxo BE+FE) ganha suite no modelo `tests/integration/test_aba_endpoints.py` (canônico, commit `4aebe36`):
- **TestSmoke** (TestClient, sem DB): 1 test por endpoint validando que existe + exige auth (401 sem service token). Roda em CI.
- **TestE2E** (`@pytest.mark.docker_demo`, httpx + psycopg real): cobre happy path em N passos numerados criar→listar→atualizar→deletar com assert `r.text` pra erro legível. Fixtures `scope="module"` criam empresa/user/dados isolados com `_RUN = uuid.uuid4().hex[:8]`, CASCADE no teardown.
- **TestE2EIsolamento** (quando RBAC envolvido): 2º user mesma empresa NÃO vê dados do 1º; PATCH/DELETE cross-user → 404.
- Headers E2E: `get_admin_api_headers()` + `X-User-Id` + `X-Empresa-Id`. Rodar com `DATABASE_URL=postgresql://postgres:postgres@localhost:5434/whatsapp_langchain INTERNAL_SERVICE_TOKEN=dev-token-change-in-production uv run pytest ...`.
- Suite verde é pré-condição pra marcar sprint como SHIPPED.

Frontend (run from `frontend/`):
- `npm run dev` / `npm run build` / `npm run start` / `npm run lint`
- Frontend needs its own `frontend/.env.local` for `npm run dev` (separate from root `.env`); Docker uses compose env vars instead.

## Architecture

The system intentionally splits into two processes that share Postgres. Understanding the boundary is the prerequisite for almost any change.

**API (`src/whatsapp_langchain/server/`)** — FastAPI HTTP edge. Validates the inbound webhook (apikey na Evolution, HMAC-SHA256 no WABA), applies per-phone rate limit, normalizes payload, and enqueues into `message_queue`. Responde em <100ms. Never invokes the agent inline. `/webhook/sync` exists for development only and is auto-disabled when `ENVIRONMENT=production`. Rate limit do webhook usa in-memory por default (single-process); em multi-instância habilite `RATE_LIMIT_DISTRIBUTED=true` para sliding window em Postgres (tabela `rate_limit_buckets`, migration `005`).

**Three middlewares are stacked in `server/main.py`** (Starlette LIFO — registered last runs first):
- `install_correlation_id` — accepts client `X-Request-Id` or generates UUID16, binds to `structlog.contextvars` so every log of the request carries `request_id=X`. Echoes the ID in response header.
- `install_admin_rate_limit` — sliding window 60 req/min per `user_id` on `/api/*` (excluding `/api/health`). Uses generic `rate_limit_bucket` (migration 022) via `shared/rate_limit.py::enforce_bucket_limit`. Skips OPTIONS (CORS preflight).
- `install_security_headers` — `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy` always; `Strict-Transport-Security` in production.

Better Auth has its own `rateLimit` config in `frontend/src/lib/auth.ts` (5 attempts/15min on `/sign-in/email`, 3/15min on `/sign-up/email`, 3/h on `/forget-password`, 5/h on `/reset-password`).

**Worker (`src/whatsapp_langchain/worker/`)** — Async loop polling `message_queue` com **N mensagens em voo por réplica** (`WORKER_CONCURRENCY`, default 1 = serial): `worker/main.py::_loop_consumo` segura um `asyncio.Semaphore(N)`, cada mensagem roda numa task própria (`_processar_mensagem`: `empresa_scope` DENTRO da task, heartbeat de lease próprio) e o loop só dorme quando o claim devolve None; SIGTERM para de reivindicar e dá `SHUTDOWN_GRACE_SECONDS` (8 s) pro que está em voo; o circuit breaker lê `WORKER_HEALTH` antes de cada claim. Pools do LangGraph e da app e o limiter do LLM (`LLM_RATE_LIMIT_REQUESTS_PER_SECOND` é POR SLOT) escalam com N. Paralelismo é só ENTRE conversas: o claim é **serializado por conversa** (`shared/queue.py::claim_next`): candidato sem lock de row → `pg_advisory_xact_lock(hash(phone:agent))` com a MESMA chave do webhook (`chave_lock_conversa`) → UPDATE re-checando `NOT EXISTS` de row `processing` com lease válido na mesma conversa. Invariante: **nunca dois turnos no mesmo `thread_id`** — antes disso (claim de `FOR UPDATE SKIP LOCKED` puro) as 2 réplicas de produção rodaram 75 turnos sobrepostos no mesmo atendimento em 30 dias (medido 2026-09-18). A ordem de locks `advisory → row` é igual à do webhook, por isso o candidato NÃO pode ser travado antes do advisory; o UPDATE de expiração (lease vencido sem tentativas) roda em transação própria. O candidato é escolhido pela **posição na fila da própria empresa** (`row_number()` por `empresa_id` + o que ela já tem em voo) antes da idade: round-robin entre empresas sem estado, FIFO dentro de cada uma — um burst do hospital não faz a PME esperar. Depois do claim, preprocesses media (image/audio → text via OpenRouter multimodal), sends the typing indicator (best-effort), invokes the LangGraph agent, sends the outbound reply via the connection's provider, *then* calls `mark_done`. **`mark_done` only runs after the outbound send succeeds** — a send failure flows into `mark_failed`'s retry path. This ordering is load-bearing for at-least-once delivery.

**Postgres is the queue** — no Redis/RabbitMQ. State machine: `queued → processing → done | failed`, with retry going `processing → queued` (with `process_after = NOW() + attempts*5s` backoff) until `attempts >= max_attempts`. Lease expiry on a stuck `processing` row promotes to `failed` only when attempts are exhausted; otherwise the row is reclaimable. Com o claim por conversa, uma row `processing` com lease válido **prende a conversa inteira** até o lease vencer (o heartbeat R7 renova a cada `LEASE_SECONDS/3` enquanto o worker vive) — por isso `LEASE_SECONDS` em produção tem que ser curto (180 s), não 5000. `lease_until IS NULL` não ocupa a conversa (é o estado que `mark_failed` deixa ao devolver a row pra `queued`).

**Contrato de mídia** (`worker/media.py::preprocess_incoming_message`, mig 164) — o agente **sempre** recebe texto, inclusive quando o arquivo não pôde ser lido: aí vem um bloco `[Arquivo recebido: <nome> — <motivo>]` e quem responde ao cliente é o agente, com as próprias palavras. **Só falha transitória** (download, provedor de visão/transcrição) devolve `should_invoke_agent=False` e volta pra fila; recusa permanente (`UnsupportedFileTypeError`, `FileTooLargeError`, `FileExtractionError`) **não** é retentada. Não reintroduzir resposta automática nesse caminho: ela saía por fora dos portões de fila do departamento, modo manual e whitelist. Documentos lidos: PDF, DOCX, XLSX (openpyxl) e DOC legado (`antiword`, apt nas imagens — ausente, degrada pro bloco acima). O nome do arquivo vem do provedor (`documentMessage.fileName` / `document.filename`) em `message_queue.media_filename`; `shared/file_extractor.py::filename_for_media_type` é a **fonte única** do mapeamento mime→nome (estava duplicada e divergiu). `agente_ia.aceita_imagem/audio/documento` governa a ingestão **e** as tools `midia.*`.

**Debounce semantics** (in `shared/queue.py::enqueue_or_buffer`) — text-only messages from the same `(phone, agent)` within `MESSAGE_BUFFER_SECONDS` are concatenated into a single queued row. Media bypasses debounce *and* flushes any pending text from the same `(phone, agent)` so the worker processes text-then-media in `created_at` order. Concurrent webhooks are serialized via `pg_advisory_xact_lock` keyed on a SHA-256 hash of `phone:agent` — **o claim do worker toma a mesma chave**, então o debounce nunca mescla texto numa row que está sendo reivindicada. O buraco entre o claim e a resposta (~4 s até a IA começar + a chamada do LLM) é coberto em dois pontos do worker: `shared/queue.py::absorver_pendentes` logo ANTES do `ainvoke` puxa pro turno atual os textos `queued` da conversa (apaga as rows e concatena no `incoming_message`, como o merge do debounce; sob o mesmo advisory lock; mídia fica pra turno próprio) — em produção 13,6 % dos turnos de IA tinham a próxima mensagem já na fila, e sob carga isso cortou 77 % das chamadas de LLM; e `existe_mensagem_mais_nova` antes de enviar engole a resposta quando algo chegou DURANTE a chamada do LLM. **Múltiplas mídias (NumMedia > 1) em um único webhook são enfileiradas como N rows independentes com o mesmo `message_id`; o worker processa cada uma como turn separado do agente; o checkpointer LangGraph agrega por `thread_id`.**

**LangGraph lifecycle is explicit, not lazy** — the worker opens `AsyncPostgresSaver` (checkpointer) and `AsyncPostgresStore` (semantic memory) once at boot via `open_checkpointer()` / `open_store()` (`shared/db.py`) using `AsyncExitStack`, then reuses them across every message. `bootstrap_langgraph_schema()` runs the LangGraph DDL at startup (gated by advisory lock `8_642_001` so API and Worker cooperate on first boot). Don't reintroduce per-request schema setup.

**Two memory contracts, runtime keys must be set together:**
- `thread_id = "{phone_number}:{agent_id}"` — checkpointer scope (single conversation history)
- `user_id = phone_number` — store scope (semantic, cross-thread; namespace `(user_id, "memories")`)
- The agent factory only exposes `save_memory` / `read_memory` tools when `store` is non-None (i.e. `MEMORY_ENABLED=true`). Conversely, `MEMORY_ENABLED=false` returns `(None, None)` from `open_store` — the agent loads without memory tools rather than failing.

**Agent contract (catalog plug-in)** — agents live at `src/whatsapp_langchain/agents/catalog/<agent_id>/` and must export:
- `agent.py::build_graph(checkpointer=None, store=None)` returning a compiled graph
- `graph.py::graph` (a top-level variable, used by `langgraph dev` / Studio)
- `prompts.py::SYSTEM_PROMPT`
- Register in `langgraph.json` under `graphs`. The `loader.py` discovers the directory; the JSON only matters for Studio.
- Agents must NOT import `server/` or `worker/` modules. Use `shared/llm.create_chat_model()` (rate-limited factory) and `agents/middleware.get_context_middleware()` (selects trim/summarize/none from `CONTEXT_STRATEGY`).

**Frontend / admin auth** — Next.js panel in `frontend/` uses Better Auth against the same Postgres in a separate `auth` schema (migrations `003_auth_schema.sql`, `004_better_auth_tables.sql`). Server-side fetches to `/api/*` go via `INTERNAL_API_URL` + bearer `INTERNAL_SERVICE_TOKEN` (enforced by `verify_service_token` dependency on the admin router). On first `/login` the frontend bootstraps the initial admin from `ADMIN_EMAIL`/`ADMIN_PASSWORD` if `auth."user"` is empty. **`INTERNAL_SERVICE_TOKEN` and `BETTER_AUTH_SECRET` must be set even locally** — `Settings.validate_runtime_settings()` raises at API startup otherwise; in production the token is also length-checked (≥32).

**Migrations** — application schema lives in `db/migrations/*.sql` (controlled by `_migrations` table; lock id `8_642_000`). LangGraph schema (`checkpoints*`, `store*`) is created in-code by `bootstrap_langgraph_schema()` at startup. Don't write SQL migrations for LangGraph tables. **Currently 180 migration files** numbered up to `186` (chronological gaps existem — não são problema; o número do arquivo ≠ contagem). Recent highlights:
- `022_rate_limit_generic.sql` — generic `rate_limit_bucket` (used by admin endpoints middleware)
- `023_hook_dead_letter.sql` — DLQ for hooks that exhaust retries
- `024_user_status.sql` — `auth.user.status` (active/disabled) blocks login + kills sessions
- `025_password_reset_pending.sql` — cache of reset links (no SMTP path, admin shares manually)
- `026_login_event.sql` — auth audit log with IP/user-agent
- `027_agendamento.sql` — local mirror of Google Calendar events with governance
- `073_atendimento_avaliacao.sql` / `074_empresa_csat_config.sql` — NPS/CSAT capture + config por empresa (ver módulo NPS abaixo)
- `096`, `100`–`103` — RLS Postgres maturidade real: 4 roles application, expand FORCE RLS pra todas as tabelas com `empresa_id`, troca policy permissive→estrita, security hardening logging (Sprint A.2; runbook `docs/RLS_OPERATIONS.md`)
- `105_asaas_billing_fields.sql` — campos Asaas em `empresa` + index pra webhook lookup (schema billing `plano`/`transacao` existe desde mig 059)
- `106_user_profile_fields.sql` — Sprint U: `auth.user.telefone`/`last_login_at`/`avatar_path` + trigger `last_login_at` (ver módulo `/api/usuarios` abaixo)
- `107_langfuse_trace_link.sql` — link bidirecional `ia_execucao`/`message_queue` ↔ Langfuse trace (worker gera `trace_id` determinístico quando `LANGFUSE_ENABLED`)
- `108_conexao_default_unique.sql` — garante 1 só conexão `is_default` por empresa (defesa em profundidade)
- `109_message_template_provider.sql` — generaliza `waba_template` → multi-provider (revertido na mig 153: só WABA)
- `110_historico_indexes.sql` — índices pro módulo Histórico (/chats repaginado: filtros + export CSV/XLSX)
- `111_usuario_conexao.sql` / `112_turnos.sql` — Sprint U: conexão padrão por usuário + turnos/jornada (gate de distribuição no `pick_best_atendente`)
- `113_campanha_template.sql` — campanha dispara template HSM aprovado (selector no form + variáveis)
- `114_twilio_legacy.sql` / `153_drop_twilio_provider.sql` — depreciação e **remoção** do Twilio: o CHECK de `conexao.provider` passa a aceitar só `waba`/`evolution`, e o de `waba_template.provider` só `waba`. `conexao.content_sid` fica órfã de propósito (rotas leem a tupla por posição)
- `115_empresa_branding.sql` — **white-label por empresa**: `empresa` += `logo_path`/`nome_exibicao`/`cor_primaria`/`cor_secundaria` (ver módulo White-label abaixo)
- `118`–`125` — **Disparador** (extensão Chrome + captura + disparo em massa): `empresa_api_key`, schema captura, jitter anti-ban, compliance, mídia/agendamento/origem na campanha
- `126_conexao_teto_diario.sql` — **anti-ban: teto diário + aquecimento por conexão**: `conexao` += `daily_send_cap`/`warmup_started_at` + tabela contadora `conexao_envio_diario`. O dispatcher reagenda a campanha pro dia seguinte ao bater o teto (warm-up, não aborta). Lógica em `shared/conexao_quota.py`; UI no painel anti-ban de `/connections/[id]`; `GET /api/conexoes/{id}/quota`
- `127`–`131` — Disparador (cont.): pausa periódica de campanha, pairing state da conexão, **desacopla atendimento da conexão** (`129`: FK SET NULL + snapshot do canal — apagar número preserva histórico), pool de conexões na campanha, evento da extensão
- `132_conexao_tipo_atendimento_default_manual.sql` — nova conexão nasce em modo **manual** (IA off por default; ligar quando configurar). Ver [Gate IA + Whitelist]
- `133_whitelist_numero.sql` — **`whitelist_numero` é lista de BLOQUEIO, apesar do nome**: número cadastrado NÃO recebe nenhuma resposta automática (sem workflow/menu/agente/typing/transcrição), em TODAS as conexões da empresa — mesmo contrato do gate de modo manual (mig 132), porém por número. Caso de uso: contatos pessoais do dono (mãe/pai/esposa) que chamam no número comercial. Gate em `worker/processor.py` via `shared/whitelist.py::is_whitelisted`, resposta marcada com `WHITELIST_BYPASS_MARKER`. O lookup gera as variantes com/sem o nono dígito BR (`candidatos_lookup`), então as duas grafias do mesmo número casam. `134_plano_pessoal.sql` — plano billing "Pessoal" R$97; `135_resumo_diario.sql` — resumo diário de atendimentos por WhatsApp (cols `resumo_diario_*` em `empresa`; worker `shared/resumo_diario.py`, claim atômico 1x/dia)
- `136_rls_catalogo_global_visivel.sql` — RLS deixa ver linhas globais (`empresa_id IS NULL`) de `modelo_llm`/`mcp_server` (USING permite NULL); `137_fewshot_source_trace.sql` — auto-dataset Langfuse: `fewshot_example` += `source_trace_id`/`fonte` + índice único parcial (idempotência do re-run)
- `138_modelos_catalogo_openrouter.sql` — **catálogo de modelos curado (OpenRouter)**: adiciona globais em `modelo_llm` (gemini-3.1-flash-lite, deepseek-v3.2, glm-4.5-air/4.7-flash, tencent/hy3-preview, qwen VL) + **corrige 3 slugs 404** (claude-haiku/sonnet/opus com traço→ponto). Fonte dos selects = `modelo_llm` (editor monta `provedor/nome`); `CURATED_MODELS` em `shared/llm.py` dirige o painel `/models`. IDs conferir SEMPRE contra `curl openrouter.ai/api/v1/models` (grok-4.1-fast foi aposentado). Módulo Testar: comparar até 4 modelos + anexar áudio/doc/imagem (reusa `worker.media.preprocess_incoming_message`)
- `166_departamento_ia_na_fila.sql` — interruptor **"IA continua na fila"** por departamento (default OFF): transferir calava o agente até alguém assumir, e fila não trabalhada deixava o cliente falando sozinho (465 msgs/semana medidas na 1018). Ligado, o worker injeta o prefixo `[JÁ ENCAMINHADO AO SETOR …]` (DEPOIS dos guardrails) e `transfer_to_human` ganha guarda anti-reencaminhamento. Handoff real continua calando. Ligado só na 1018/999011
- `167_convite_acesso.sql` — **convite de acesso por WhatsApp**: link de definição de senha (Better Auth, 1h, uso único) em vez de repassar a senha à mão. `auth."user".convite_enviado_at` + `shared/telefone.py::normalizar_br` (país+DDD obrigatórios; `+55996460034` sem DDD é recusado na ENTRADA). Página `/reset-password` criada (o redirectTo da mig 025 sempre apontou pra um 404). **O link NUNCA entra em log/auditoria/resposta** (testado). Gotcha: o Better Auth desta versão expõe `/request-password-reset` — `/forget-password` dá 404, e a regra de rate limit mirava o caminho morto (corrigido)
- `168_dispositivo_push.sql` — **push FCM pro app Android**: tabela `dispositivo_push` (token UNIQUE, UPSERT troca o dono), `shared/push_fcm.py` (FCM v1 + service account via `FIREBASE_SERVICE_ACCOUNT_JSON`; vazio = loop nem inicia), `shared/push_loop.py` (LISTEN no MESMO canal da mig 035/145, só `kind=inbound`, filtra outbound/nota no SELECT), rotas `/api/push/registrar|remover`. Data-message only — o app colapsa por `tag=atendimento_id`. Token inválido (404/UNREGISTERED e 400 de token malformado — permanente) é removido. Validado fim a fim em produção 2026-08-13
- `169_transcricao_audio.sql` — **transcrição de áudio pro OPERADOR** (independente do agente IA, que só transcrevia no preprocess — conexão em modo manual/whitelist/menu retorna ANTES e a nota de voz nunca virava texto no painel): `message_queue.transcricao` (≠ `normalized_input`, que é input DO AGENTE) + `conexao.transcrever_audio_sempre` (default OFF; painel `/connections/[id]`). Helper `shared/transcricao.py::transcrever_mensagem` (idempotente; `UPDATE ... WHERE transcricao IS NULL` desempata corrida), endpoint `POST /api/atendimentos/{id}/mensagens/{mid}/transcrever` (escopo de empresa), gancho no worker ANTES dos gates (best-effort; se o fluxo seguir ao agente, o preprocess reusa via `transcricao_previa` — sem pagar a mesma chamada 2x). Timeline web e app exibem/disparam. Suite `tests/integration/test_transcricao_endpoints.py` (E2E testa idempotência SEM chamar OpenRouter)
- `176_empresa_voz.sql` / `177_plano_feature_voz.sql` — **voz do agente**: cliente que manda áudio recebe nota de voz (`empresa.voz_ativa/voz_nome/voz_estilo`, default OFF, feature de plano Pro/Enterprise; só Evolution, via duck-typing `send_audio`). `shared/voz.py` fala pelo OpenRouter (`settings.tts_model`) e converte PCM16→OGG/Opus 48kHz com PyAV. **`gpt-audio-mini` NÃO é um TTS — é chat que fala**, e o OpenRouter não tem TTS nenhum (`/audio/speech` dá 400). Texto na mensagem `user` era lido como fala dirigida ao modelo e ele RESPONDIA em vez de ler (incidente do atendimento 63: cliente perguntou "quem é você?" e recebeu áudio que não respondia). Daí duas defesas, **as duas load-bearing**: (1) o texto a falar vai no `system` dentro de `<texto>`, com o `user` carregando só a ordem — inverter isso reintroduz o bug; (2) `verificar_fidelidade=True` transcreve a nota de voz de volta e compara com o original antes de enviar (limiar `VOZ_FIDELIDADE_MINIMA=0.85` + detector de preâmbulo), e `VozInfielError` faz o worker cair pro texto. O worker sempre verifica (o áudio SUBSTITUI o texto — divergência é resposta perdida); a amostra da UI não. Custo da síntese é registrado ANTES da recusa, e a transcrição de conferência entra com `finalidade=voz_verificacao`
- `178`–`180` — **módulo Saúde de IA** (branch `feat/saude-ia-f1-catalogo-openrouter`): catálogo OpenRouter COMPLETO dentro do Nexus (mig 178: `openrouter_provedor/modelo/endpoint_metrica/sync_estado`; sync 10min no worker + 1x/dia catálogo; latência/throughput SÓ populam COM chave — sem auth vem null em silêncio), rankings diários (mig 179: cache do dataset oficial `/datasets/rankings-daily`; `model_permaslug` vem DATADO e a data fica ANTES da variante — `minimax-m3-20260531:free` → `minimax-m3:free`; linha `other` fora do ranking mas no denominador do share) e alertas de degradação (mig 180: `ia_alerta`, detecção determinística em `shared/ia_alertas.py` — limiares uptime<97%/latência>2× baseline 7d/throughput<50%/erros próprios>20% — dedup por episódio, cooldown 6h anti-flap, auto-resolve; notifica WhatsApp da empresa 1 pelo caminho do resumo diário + Telegram opcional via env + banner em `/catalog/openrouter`; checagem `ia_alertas` no relatório de produção). Rotas `/api/openrouter/*` superadmin-only (recurso de PLATAFORMA, padrão mig 173); catálogo completo é observabilidade — empresa continua no curado, promoção explícita
- `186_response_media_filename.sql` — **nome real do anexo enviado pelo operador**: a mig 164 guardava só o nome do que o CLIENTE manda; o `/responder-midia` recebia `arquivo.filename`, passava ao Evolution e descartava — a timeline mostrava "application/pdf". Agora `_persist_outbound_row(media_filename=…)` grava (saneado por `nome_de_arquivo_seguro`: só o nome-base, ≤255; nota de voz fica NULL), `list_atendimento_mensagens` expõe `media_filename` E `response_media_filename` (lista e retorno do POST — as duas serializações), `get_mensagem_midia` devolve `(bytes, mime, nome)` e o endpoint `/midia` manda `Content-Disposition: inline; filename*=UTF-8''…` (o proxy do Next repassa) — "abrir/baixar" salva `orçamento.pdf`, não o id. Web: `timeline.ts` → `MediaPreview nome=`. Os 4 testes de `test_atendimento_helpers.py` que estavam vermelhos desde a mig 184 (mock com 22 colunas) voltaram a passar com `_COLUNAS_MENSAGEM = 25`
- **Leva fila de atendimento (2026-09-01, PR #116 — SEM migração)**: (1) login cai direto em `/atendimento` — decisão por permissão em `frontend/src/app/page.tsx` (`atendimento.read` c/ variantes; sem a perm → dashboard), tour de 1º acesso reescrito pra ensinar a USAR a fila; (2) fila 300px com pontos de situação/prioridade (`SITUACAO_PONTO`, só tokens do tema) + sentimento + filtro `?assigned_to=` (kwarg novo em `list_atendimentos`); (3) copiar/editar/apagar mensagem na timeline WEB (`bolha-menu.tsx` — backend da mig 172 já existia; edição reusa o composer, exclusão mútua com nota interna; **no DEV o envio mock esconde Editar/Apagar de propósito** — chave `mock-evo-` → flags false em `avaliar_alteracao_resposta`); (4) fila viva: proxy `/api/sse/empresa` (novo) sobre `GET /api/atendimentos/events` + `fila-live.tsx` (refresh debounce 2s, beep WebAudio + Notification, sino c/ localStorage; **SSE não abre mais conexão PG por stream**: `shared/notify_hub.py` mantém UM `LISTEN` por canal por processo e faz fan-out em memória — 20 streams = 1 conexão; reconexão com backoff reemite `connected` e os clientes ressincronizam) e rota `POST /{id}/marcar-nao-lido` (DELETE do read receipt); (5) preview da última mensagem no card (`derivar_preview` pura; `MARKERS_INTERNOS` é a fonte única dos prefixos internos do worker — nota interna filtrada no SQL, sem base64 no SELECT); (6) atalho `/` no composer vazio abre busca cmdk dos modelos. Gotcha novo do deploy: Dokploy leva minutos entre "Deployment queued" e o recreate — "Up N days" logo após o run verde é fila, conferir de novo antes de alarmar. Suite `tests/integration/test_nao_lido_endpoints.py`
- **Inbox agrupado — backend (2026-09-18, SEM migração)**: (1) `Atendimento.aguardando_desde` (derivado, `derivar_aguardando_desde` pura em `shared/atendimento.py`) — `created_at` da última row visível quando o CLIENTE falou por último (texto ou mídia) e ninguém respondeu; resposta real ou mídia do operador zera, marker de `MARKERS_INTERNOS` NÃO conta como resposta, nota interna já fica fora pelo SQL do lote (a MESMA query `DISTINCT ON` do preview alimenta os dois — não abrir outra). Row `queued`/`processing` conta como pendente de propósito. O card mostra "Sem resposta há X" a partir daqui; não é SLA por departamento (`departamento.tolerancia_atend_inativo_min` segue sem uso). (2) `q` da listagem com ≥4 dígitos também casa `regexp_replace(c.telefone, '\D', '', 'g') LIKE '%dígitos%'` — em produção `cliente.telefone` é E.164 COM `+` (2008/2008) e no dev há mistura; normalizar os dois lados é o que faz "(67) 99979-1234" achar "+5567999791234". Piso de 4 dígitos pra "Ana 12" não varrer telefones. Suite `tests/integration/test_aguardando_desde_endpoints.py`
- **Inbox agrupado — frontend (2026-09-18, `frontend/src/app/atendimento/`)**: a fila de `/atendimento` virou lista AGRUPADA (Claude Design "Inbox Agrupado v2", mesmo design system). Agrupamento é **client-side sobre a página carregada** (≤50 itens; contagem por grupo é da página, o rail continua com `/contadores`): `agrupar.ts` (puro) por status (ordem fixa: resposta não enviada → meus → humano solicitado → aguardando atendimento → em atendimento → com a IA → resolvidas/abandonadas; "meus" = `assigned_to_user_id === userId` e vence os abertos), por departamento (nome via `departamentos` da page) ou por responsável (nome via `["atendentes"]`, consultado só nesse modo). Rail com 3 itens (Não Resolvidas com badge `nao_lidas` / Resolvidas / Todas) — `?tipo=nao_lidas` e `?tipo=humano_solicitado` seguem como deep link. Preferências por navegador em `localStorage` via `hooks/use-local-storage.ts` (`useSyncExternalStore`, sem setState em efeito — o React Compiler reprova): `atd-agrupar-por`, `atd-grupos-abertos` (IA e "em atendimento" nascem fechados), `atd-largura-lista` (alça 320–860px, duplo clique = 440) e `atd-largura-rail` (200–400, padrão 256; `hooks/use-colunas-redimensionaveis.ts` + `alca-coluna.tsx`, pegador visível — arrastar pra esquerda dá o espaço à conversa). **Conversa aberta = `?id=` na URL**, escrita com `window.history.replaceState` (o App Router sincroniza `useSearchParams` SEM ir ao servidor — `router.replace` refaria os 4 fetches da página); rail e "Limpar" preservam o `id`; `page.tsx` não o lê. Ação de estado no painel (atender/devolver/transferir) NÃO fecha mais a conversa: `AtendimentoDrawer.onAcaoConcluida` invalida `["atendimentos"]`+`["contadores"]` e o cabeçalho reflete o novo estado; resolver/abandonar fecha (`{ encerra: true }`). Busca (nome/telefone/protocolo) mora na `lista-toolbar.tsx` com debounce de 400 ms → `?q=` (o servidor filtra; `router.replace`). Gotchas: (1) a coluna da conversa é `w-0 flex-1` e não só `min-w-0` — a largura intrínseca do drawer subia até o `<main>` do app-shell (sem `min-w-0`) e a página vazava 106px a 1440 com a lista em 560; (2) `docker compose up -d --build frontend` no dev também reconstrói a **api** (dependência) a partir do checkout atual — trocar de branch antes do build troca o backend do dev sem avisar. Só UM drawer montado continua valendo (`useMediaQuery` → `undefined` no 1º render). **Paginação**: a lista é `useInfiniteQuery` por `offset` (50 por página, 1ª página = render do servidor) com botão "Carregar mais (N carregadas)" — antes a API cortava em 50 e a lista agrupada escondia o resto sem avisar (no dev: 118 abertas, "Humano solicitado" mostrava 1 de 7); páginas são deduplicadas por id (offset sobre lista viva repete item que subiu) e a invalidação do SSE refaz as páginas carregadas.
- **Conversa compacta (2026-09-19, `frontend/src/app/atendimento/`) — "menos interface, mais conversa"**: a conversa (drawer no celular, coluna no desktop) ficou só com o que a interação imediata precisa — cabeçalho de 48px (`cabecalho-conversa.tsx`: ← · avatar · nome · chip de situação · **ação principal** por estado (aguardando → Atender, em andamento → Resolver) · ⋮), a timeline ocupando o resto e o composer de UMA linha (`+` · campo que cresce até ~6 linhas · microfone · ➤). No celular a timeline passou de ~54 % para ~85 % da altura. Nada foi removido, saiu da área permanente: **painel de informações** (`info-conversa.tsx` — contato, protocolo/id, badges, tags da conversa e do cliente, triagem da IA, coleta prévia, último atendimento, arquivos) abre a um toque no avatar/nome ou no ⋮, como 3ª coluna quando a coluna da conversa tem ≥ 880px (`hooks/use-largura-elemento.ts`, ResizeObserver — media query não serve entre dois sidebars e uma lista redimensionável), sheet lateral quando não tem, bottom sheet no celular; preferência em `atd-info-aberta` só no desktop. O ⋮ (DropdownMenu do kit) reúne Contato / Atendimento (Devolver à IA, Transferir → `transferir-dialog.tsx`, Resolver, Abandonar) / Inserir modelo / Atualizar / Avançado (números sem IA, resetar conversa); `confirm()`/`alert()` viraram `ConfirmDestrutivo`/toast. O `+` do composer (`composer-menu.tsx`) leva Anexar, Template, Inserir modelo e o interruptor **Nota interna** — em modo nota o composer fica âmbar com faixa "não vai para o cliente". **Timeline** (`timeline.ts`, puro): bolhas consecutivas do mesmo remetente e dia (≤ 30 min entre elas) viram UM grupo com um cabeçalho "Cliente · 15:08" ("17/09 · 15:09" em dia anterior; data completa/remetente/id no `title` e no menu da bolha); remetente da saída vem de `normalized_input` (`manual:<user>` = Operador, `manual:system:` = Sistema, resto = IA); os markers do worker viram **um chip por sequência** (`aviso-ia.tsx`: "IA bloqueada · 3", "IA desligada", "Na fila do setor", "Sem agente de IA") com popover de motivo + "Reprocessar com IA" por mensagem — antes cada mensagem pulada trazia uma linha inteira em caixa alta e, pior, `[fila` e `[resposta superada` caíam no rótulo da lista de bloqueio; `[resposta superada` não gera aviso e `handoff` também não (o cabeçalho já diz "Em atendimento"). **Faixa de triagem** (`faixa-triagem.tsx`, ~28px sob o cabeçalho, só quando a IA classificou): `🤖 alta · frustrado · classificação · 1ª linha do resumo ›` — é o contexto que o operador precisa ANTES de responder, por isso não foi pro painel; tocar abre o painel na seção da triagem, o ✕ esconde por atendimento (`atendimento-card-collapsed:triagem-faixa-<id>`) e o link no painel traz de volta; chips de prioridade/sentimento têm fonte única em `situacao.ts` (`PRIORIDADE_CHIP`/`SENTIMENTO_CHIP`). No cabeçalho o nome tem prioridade sobre o chip de situação (`shrink-0` até 60%). Gotchas: (1) o teto de largura da bolha fica no invólucro do `BolhaMenu`, não na bolha — porcentagem contra wrapper que encolhe ao conteúdo quebrava o texto cedo; (2) `<audio>` dentro da bolha precisa de largura fixa (`w-64`), `w-full` colapsava a zero; (3) `data-[side=bottom]:h-auto` do `SheetContent` vence `h-*` simples — a altura do bottom sheet vai na variante; (4) `TagPopover` virou `Popover` do kit (portal) porque `absolute` ficava cortado dentro do painel rolável; (5) texto sem espaço ("Jsjsjsdjss…") estourava a tela: bolhas/legendas levam `wrap-anywhere` e o `Textarea` do kit ganhou `min-w-0 wrap-anywhere` — com `field-sizing-content`, a palavra inquebrável virava a largura mínima do campo em flex/grid (composer E o diálogo "Nova conversa"); (6) o `*:focus-visible { outline }` de `vsa-design-tokens.css` era SEM camada e vencia o `outline-none` de todo input do kit — cada campo focado tinha anel + outline (dois retângulos na busca da fila no celular); a regra mora agora dentro de `@layer base` em `globals.css`, onde o utilitário vence.

**⚠️ Variável volátil no meio do prompt mata o cache (custo de LLM)** — o cache de prompt do Gemini/OpenRouter guarda o **prefixo comum mais longo** da requisição, cobrado a 0,25× o input normal. Ele para na PRIMEIRA diferença entre uma chamada e a seguinte. Como `render_template` resolve `{{data.*}}`/`{{var.*}}` **dentro** do system prompt, uma variável volátil no meio dele torna o texto renderizado único a cada mensagem e joga fora tudo o que vem depois. Medido no agente 80 (empresa 1018, 2026-09-10): `{{data.agora}}` (= `strftime("%H:%M")`, muda por minuto) no caractere 4.242 de 19.588 → **958 tokens cacheados de 7.189 de input**, exatamente onde o relógio entrava. Movendo o bloco `<avisos_do_dia>`+`<horario_de_atendimento>` para o FIM (só relocação — 239 linhas antes, 239 depois, zero alteradas), o prefixo estável foi de ~1.010 para **~4.770 tokens**. Regra: **conteúdo volátil vai no fim do prompt, nunca no meio** — volatilidade é o que conta, não a presença de `{{}}` (um `{{var.LINK_PORTAL}}` que renderiza igual todo dia não atrapalha). Ordem de volatilidade: `data.agora`/`data.now_iso`/`data.instrucao_expediente` (minuto) > `data.hoje`/`data.dia_semana`/avisos do dia (dia) > `var.*` de configuração (quase nunca). Não confundir com a **estratégia de memória** do agente (Window/Buffer/Summary/Sem memória): ela governa o que o agente LEMBRA, e o `Window` já poda o estado de verdade (`RemoveMessage`, `middleware/trim.py`) — no agente 80 o histórico é fatia pequena perto dos 4.897 tokens de prompt fixo, então trocar a estratégia não mexe no custo dominante. TTL do cache implícito é ~3-5 min, então conversa com pausa longa perde o cache de qualquer jeito.

**⚠️ Nada pesado no `configurable` do LangGraph (checkpoints = 65% do banco)** — o `AsyncPostgresSaver` copia **todo** o `config["configurable"]` para `checkpoints.metadata` em cada checkpoint do run. `worker/processor.py` passava `"media_url": message.media_url`, e mídia inline da Evolution é `data:...;base64` com o arquivo inteiro: medido em 2026-09-15, `metadata` = **1.968 MB** (uma linha com 71 MB) contra 10 MB na coluna `checkpoint` — o State está limpo, o `Window` poda de verdade; a porta é o `configurable`. O conselho comum "não salve mídia no State" não cobre isso. Regra: `configurable` leva **referências** (`message_queue_id`, ids), nunca conteúdo: `worker/processor.py::_montar_configurable` só deixa passar `media_url` http(s), e as tools de `agents/tools/midia.py` leem `message_queue.media_url` pela referência. Medido no dev (2026-09-15, mesmo áudio de 36 kB): `metadata` por checkpoint caiu de 36.918 para **311 bytes**. Podar checkpoints antigos é seguro: o app só usa `aget_state` (nunca `get_state_history`). Plano em `.planning/reports/20260915-checkpoints-langgraph-plano.md`.

**Outbound manual** (`shared/outbound.py::send_outbound_manual`) — used by composer in `/atendimento` drawer. Routes by `Conexao.provider` via `_build_client()`: `waba` **com `waba_phone_id` → `WabaClient`** (Cloud API própria, `integrations/waba/client.py`), `evolution` → `EvolutionClient`. Conexão `waba` **sem** `phone_id` é linha legada sem saída: falha com erro explícito mandando refazer o Embedded Signup. Same `OutboundClient` Protocol the worker uses (`worker/outbound_client.py`).

**Gate de opt-out no disparo** (`shared/campanha.py`) — `disparador_opt_out` é consultada em **quatro** portas, não uma: `create_campanha`, `add_destinatarios`, `clonar_campanha` (filtro no próprio `INSERT..SELECT`) e o `_dispatch_loop`, que repete a checagem **por lote**. A quarta é load-bearing: `_reagendar_warmup` espalha a campanha por dias e o STOP chega no meio dela. Até 2026-09 a supressão só era consultada no `preview_disparo` — quem criava campanha pelo painel enviava para quem pediu descadastro. No disparo o suprimido é REMOVIDO em vez de mudar de status: o CHECK de `campanha_destinatario` só aceita `pendente|enviado|falhou`, marcar `falhou` contaminaria o kill-switch e deixar `pendente` faria o lote ser relido para sempre. `total_destinatarios` nasce descontado — se contasse o suprimido, `_mark_finished` (que compara `enviados == total`) fecharia toda campanha como `partial`.

**Providers de saída: WABA e Evolution, só** (mig 153) — o Twilio saiu inteiro em 2026-09 (cliente, webhook `/webhook/twilio`, Content API, settings, dependência). O CHECK de `conexao.provider` aceita `waba|evolution` e o de `waba_template.provider` só `waba`; `PROVIDERS_SUPORTADOS` em `shared/models.py` espelha isso para a API recusar com 422 legível em vez de CheckViolation. Conexão `waba` **sem** `waba_phone_id` era o fallback que ia pelo Twilio — hoje falha explícito mandando refazer o Embedded Signup. As migrations 009/072 seguem semeando a conexão `twilio_sandbox` que a 153 apaga, nessa ordem: **não reescreva migration aplicada**. `conexao.content_sid` ficou órfã de propósito (as rotas leem a tupla por posição).

**Hooks dispatcher** (`shared/hook_dispatcher.py`) — fire-and-forget with **retry exponencial (1s, 5s, 25s) + DLQ**. Each attempt is logged in `hook_log`; if all attempts fail, the event lands in `hook_dead_letter` (migration 023). Admin can list/retry/archive via `GET/POST /api/hooks/dead-letter[/{id}/retry|archive]`. `EVENTOS_VALIDOS` currently lists 7: `mensagem.recebida`, `atendimento.{aberto,atendido,fechado,transferido}`, `agendamento.criado`, `agendamento.cancelado`.

**Calendar Agent v2** (`shared/calendar_integration.py`, `shared/agendamento.py`) — local source-of-truth in `agendamento` table (migration 027). `create_event` does INSERT local → POST Google → UPDATE local with `evento_id_externo` + dispatch hook `agendamento.criado`. If Google fails, marks local as `cancelado` and logs `agendamento_drift_*` (compensating action). 7 tools exposed to the agent when `calendar_enabled`: `get_current_time`, `list_calendars`, `set_active_calendar`, `list_events`, `find_free_slots`, `create_event`, `cancel_event`. Plan in `/home/opc/.claude/plans/como-est-a-base-jazzy-sketch.md` documents 5 sprints (S1+S2 done, S3-S5 pending: rules, WhatsApp approval, sync+audit).

**Transferência de atendimento** (`server/routes/atendimento.py::transfer`, `shared/atendimento.py`) — endpoint `POST /api/atendimentos/{id}/transfer` aceita `TransferInput` com `user_id` OU `departamento_id` (validator garante exatamente um). Modo atendente: `transfer_atendimento` muda `assigned_to_user_id`, mantém status `em_andamento`. Modo departamento: `transfer_atendimento_to_departamento` muda `departamento_id`, **limpa `assigned_to_user_id` e volta status pra `aguardando`** (atendimento entra na fila do depto, qualquer atendente do depto puxa via `claim`). Após transferência pra depto, `send_outbound_manual` envia WhatsApp pro cliente: "Você foi transferido para o setor *{nome}*" (best-effort). Hook `atendimento.transferido` carrega `departamento_nome` resolvido. Drawer `/atendimento` tem popover responsivo com 2 modos — modo "atendente" lista atendentes online via `loadAtendentesOnlineAction` (filtra `is_active && atendente_status='online'`) com count de atendimentos abertos pra ajudar a escolher.

**NPS / Pesquisa de Satisfação** (`shared/avaliacao.py`, `server/routes/relatorios_nps.py`, frontend `/dashboard/qualidade`) — captura automática 0-10 ao fechar atendimento, com comentário follow-up opcional (60s). Config 100% no cadastro da empresa (4 cols `csat_*` da mig 074, NÃO mais em `menu_item`). `trigger_csat_se_ativo(pool, empresa_id, atendimento_id)` é chamado em 2 lugares: (1) endpoint `POST /atendimentos/{id}/close` quando `status='resolvido'` (operador resolve via painel); (2) worker `_send_csat_se_configurado` quando cliente digita "encerrar atendimento" via WhatsApp. Captura via `_try_capture_avaliacao` no início de `process_message` — antes de menu/agente IA. Estado em `atendimento.aguardando_avaliacao_at` (24h) → grava nota → set `aguardando_comentario_at` (60s) → grava comentário (UPSERT). Tabela `atendimento_avaliacao` UNIQUE 1:1 com atendimento, snapshot `assigned_to_user_id` + `departamento_id` no momento do INSERT pra preservar ranking. 4 endpoints `/api/relatorios/nps` (geral/por-departamento/ranking-operadores/avaliacoes) com cálculo NPS clássico (`%promotores − %detratores`). Veja [docs/NPS.md](docs/NPS.md).

**Auth & user management**:
- `auth.user.status` (active|disabled) — admin toggles via UI in `/companies/[id]/members`. `frontend/src/lib/auth.ts::databaseHooks.session.create.before` blocks login if disabled; `set_user_status()` deletes `auth.session` rows on disable to expire active cookies in <30s.
- Reset password without SMTP — Better Auth callback `sendResetPassword` persists URL+token in `auth.password_reset_pending` (1h expiry). Admin retrieves via `generateResetLinkAction()` and shares via WhatsApp/Slack/whatever. UI button (KeyRound icon) in members list.
- Login audit — `databaseHooks.session.create.{before,after}` write to `auth_login_event` (migration 026). Best-effort (errors don't break login). Viewer at `/settings/security/login-history` (admin sees all, non-superadmin sees own).
- **reCAPTCHA no login** (2026-09-19, `frontend/src/lib/captcha.ts`, plugin `captchaLogin()`): com `RECAPTCHA_SITE_KEY` o `/login` executa o reCAPTCHA Enterprise (pontuação, invisível) e o `/sign-in/email` exige o token `x-captcha-response` **só quando o pedido tem `Origin`** (navegador) — o app Android passa sem. Verificação por `RECAPTCHA_API_KEY`+`GOOGLE_CLOUD_PROJECT_ID` (createAssessment) ou `RECAPTCHA_SECRET_KEY` (siteverify legado). Google fora do ar → login liberado com log. Detalhes em `docs/AUTH.md`.
- Google SSO — opt-in. If `GOOGLE_OAUTH_CLIENT_ID/SECRET` are set, `socialProviders.google` is enabled in Better Auth and login form shows the Google button. Reuses Calendar OAuth Client; just add redirect URI `https://<domain>/api/auth/callback/google` in Google Console.
- **User CRUD module** (`server/routes/usuarios.py`, `shared/usuarios.py`, frontend `/usuarios`) — Sprint U. `/api/usuarios` (perm `empresa.member.add`) é o caminho **preferido** sobre o legado `/api/empresas/{id}/membros` (não remova o legado). Endpoints: list enriquecida c/ filtros, detalhe, criar (Better Auth `auth.user` + `empresa_membro` membership), atualizar (nome/telefone/perfis/deptos), `POST /{id}/avatar` (upload local em `AVATARS_DIR`, default `/app/uploads/avatars`, ≤2MB, png/jpeg/webp/gif), `POST /{id}/sessions/invalidate` (força re-login após reset de senha), `exists/{id}`. Campos custom (`telefone`, `last_login_at`, `avatar_path`) ficam fora do Better Auth — acessados via SQL direto (mig 106).

**Bootstrap admin** (`frontend/src/lib/bootstrap-admin-core.ts`) — on first login, when `auth.user` is empty, creates the user from `ADMIN_EMAIL`/`ADMIN_PASSWORD` PLUS `empresa_membro` row (empresa_id=1, role=admin, is_default=true) PLUS `is_superadmin=true`/`emailVerified=true`. Without this triple-insert, the user logs in but every `/api/*` returns 403 because `get_empresa_context` requires membership or superadmin. Don't drop these inserts when refactoring.

**White-label por empresa** (mig `115`, `routes/empresa_admin.py`, `shared/empresa.py`, frontend `companies/empresa-form.tsx` + `layout.tsx` + `sidebar.tsx`) — cada empresa sobe **logo + nome de marca + cores** próprias, exibidas no topo do sidebar (substitui "Chat Nexus"/`/vsa-logo.png`). Espelha o padrão de avatar do Sprint U: `POST /api/empresas/{id}/logo` (admin, Pillow PNG ≤512px) → `LOGOS_DIR` (default `/app/uploads/logos`) → `update_empresa(logo_path=...)`; mount `/uploads/logos` (StaticFiles) + volume `logos_data`. `layout.tsx::resolveEmpresaUI` resolve a empresa ATIVA (cookie `active_empresa_id`) → passa `brand{nome,logo_path}` por `AppShell`→`Sidebar` (fallback default quando sem logo). As **cores** viram CSS vars `--brand-primary`/`--brand-secondary` (+ `-light`/`-dark` via `color-mix`) injetadas num `<style>` no `<head>`, sem tocar nos 3 temas. Login mantém marca padrão (sem mapeamento subdomínio→empresa).

**⚠️ `/uploads/*` e o rewrite do Next (gotcha load-bearing)** — o front serve `/uploads/avatars/*` e `/uploads/logos/*` via `next.config.ts::rewrites()` proxiando pra `INTERNAL_API_URL`. Como `output: "standalone"` **congela a destination do rewrite no route-manifest em BUILD time**, `INTERNAL_API_URL` TEM que existir no `npm run build` — por isso é passado como **`ARG` no `Dockerfile.frontend`** (+ `build.args` no compose), não só como env de runtime. Sem o ARG, cai no fallback `http://localhost:8000` e o proxy quebra com `ECONNREFUSED` (500) em produção. Regra geral: **rewrite que depende de env precisa do env no build**, não só no runtime.

## Branch / patch layout (didactic)

The repo is structured as a teaching harness across phases (`Fase_1` → `Fase_4`). The `patch/Fase_N/` directory holds drop-in replacements (`pyproject.toml`, `uv.lock`, Dockerfiles, `vsa_tech/graph.py`) meant to be copied over after `git checkout Fase_N`. Don't merge or "clean up" `patch/` — see `patch/README.md` for the intended workflow. The Docker patches matter because `make up` rebuilds against `uv.lock`, and a clean build without the patched lockfile can resolve incompatible transitive deps.

## Configuration

All config flows through `pydantic-settings` in `shared/config.py` as a singleton `settings` object — import that, don't read env vars directly. The full env surface is documented in `.env.example`. Notable defaults that matter at runtime: `CONTEXT_STRATEGY=trim`, `MEMORY_ENABLED=true`, `MAX_ATTEMPTS=3`, `LEASE_SECONDS=60`, `RATE_LIMIT_PER_HOUR=30`, `RATE_LIMIT_DISTRIBUTED=false`, `MESSAGE_BUFFER_SECONDS=2.0`, `FRONTEND_ORIGINS=http://localhost:3000`. All LLM, embeddings, and audio transcription go through one OpenRouter key (`OPENROUTER_API_KEY`). Rate limit: por default usa dict in-memory por processo (`RATE_LIMIT_DISTRIBUTED=false`); em implantações multi-instância ative `RATE_LIMIT_DISTRIBUTED=true` para sliding window em Postgres via tabela `rate_limit_buckets` (migration `005_rate_limit_buckets.sql`).

`Settings.validate_runtime_settings()` enforces three invariants no startup: (1) `INTERNAL_SERVICE_TOKEN` não pode ser vazio; (2) em produção, o token deve ter ≥32 caracteres; (3) em produção, `FRONTEND_ORIGINS` deve ter pelo menos uma origem configurada — sem isso o CORS nega todos os requests cross-origin e o painel quebra silenciosamente. O startup falha imediatamente em qualquer desses casos.

## Stress testing (Locust)

Locust profile in `stress/` (Dockerfile + locustfile.py):
- `make stress-evolution` (default) — sends `/webhook/evolution` JSON payloads
- Defaults `-u 10 -r 2 -t 60s` against `https://api.vsanexus.com`. Sobrescrevíveis via `USERS=20 RATE=5 TIME=120s HOST=https://other.url`.
- Fallback Docker (`stress-evolution-docker`) for environments without `uv` — builds image from `stress/Dockerfile`.

When stress-testing for real worker throughput, temporarily set `EVOLUTION_OUTBOUND_MODE=mock` and `RATE_LIMIT_PER_HOUR=500` in env to avoid Evolution rejecting fake numbers (400) and rate limit blocking. Revert after.

## Reference docs

When in doubt, prefer these over inferring from code:
- `docs/ARCHITECTURE.md` — full data flow + endpoint inventory
- `docs/ADDING_AGENTS.md` — the agent contract above, with examples
- `docs/DATABASE.md` — schema overview + ready-to-run inspection queries (queue, conversation, memory, checkpoints)
- `docs/EVOLUTION.md` — Evolution API provider (M2.b)
- `docs/WABA_SETUP.md` — passo a passo completo do app Meta + Embedded Signup (FB JS SDK): criar app, config_id, domínios, webhook, envs Dokploy, troubleshooting "tela branca"
- `docs/AUTH.md` — Better Auth + user status + reset sem SMTP + login history + SSO Google + rate limits
- `docs/NPS.md` — módulo de pesquisa de satisfação: schema, fluxo de captura, endpoints de relatório, dashboard
- `docs/LANGSMITH.md` — datasets + LLM-as-judge + sync sandbox 999
- `docs/ADR-004-seletor-modelos-agente.md` — **planejado, não iniciado (20/09/2026)**: aba "Modelo & Estilo" do agente vira seletor estilo Chatvolt sobre o catálogo COMPLETO (`openrouter_modelo`), créditos estimados/msg (1 C = US$0,001; chars/4 × preço prompt + 300 × preço completion), Premium só visual, e `agente_ia.contexto_tamanho` (Lite 6k … Extended 300k chars, mig 187) que **passa a ser honrado pelo worker** — hoje `tipo_memoria`/`janela_memoria` são salvos e ignorados pelo `loader.py`/`build_graph`. Ordem de PRs em `.planning/design/seletor-modelos-chatvolt/PLANO.md`
- `docs/ADR-003-data-fetching-painel.md` — TanStack Query sobre **Server Actions/proxy** (nunca axios no cliente: vazaria o `INTERNAL_SERVICE_TOKEN`); SSE invalida chave em vez de `router.refresh()` (incidente de 1073 req/min); socket.io rejeitado (não resolve o gargalo de 1 conexão PG por operador). **Resultado medido**: 1 aba parada 32→4 req/min, 4 abas + 6 eventos 401→56. Os dois custos escondidos eram fora do piloto: `nextCookies()` do Better Auth <1.6.2 re-renderizava a rota em TODA Server Action (sonda `cookies().set()`), e cada action re-prefetchava os 11 `<Link>` visíveis (`prefetch={false}` nas abas e no menu). Regra: **medir na API (`rate_limit_bucket`), não no DevTools do browser**
- `docs/ADR-001-roteamento-openrouter.md` — política de roteamento de provedores: peso aberto ganha piso de quantização (`shared/llm.py::provider_preferences`); proprietário fica SEM bloco `provider` de propósito (preferência explícita desliga o failover do OpenRouter); por que NÃO usamos `data_collection: deny` nem fallback de modelo
- `docs/DOKPLOY.md` — passo a passo completo Dokploy (Project + Compose service + Domains + envs); inclui caveat do bug do `addPrefix` quando usa `path != "/"`
- `docs/DEPLOY.md` / `docs/RAILWAY.md` — alternative deploy targets
- `docs/STRESS_TESTING.md` — Locust setup (`stress/` profile in compose)
- `docs/MIGRACAO_SERVIDOR.md` — **runbook para trocar de servidor sem re-parear WhatsApp**. O backup diário cobre só o banco da app; `scripts/exportar_producao.sh` leva também o banco da **Evolution** (tabela `Session` = credenciais Baileys; o container não tem volume, então o banco É a sessão), o banco do **Dokploy** (as variáveis de ambiente de produção — o repo só tem `.env.example`), `/etc/dokploy`, units systemd e o `rclone.conf`. Dois pontos load-bearing: (1) copiar `evolution_pgdata`/`dokploy-postgres` como arquivo, a quente, produz volume rasgado que não restaura — tem que ser `pg_dump` de dentro do container; (2) o Baileys aceita UMA conexão por sessão, então subir a Evolution nova com a antiga viva derruba uma das duas com `conflict: replaced` — a ordem de corte (parar a antiga → dump a frio → subir a nova → nunca religar a antiga) é requisito, não preferência. `--verificar` restaura em base descartável e confere conteúdo (linhas em `Session` com credencial de verdade, `compose.env` do Dokploy com bytes), porque dump truncado também tem tamanho
- `docs/BACKUP.md` — as três cópias do dump (host / Google Drive via rclone / dev), setup do remoto e a checagem `backup_offsite` do relatório
- `docs/SEGURANCA.md` — postura de segurança + red team 2026-09-16 (2 críticos, 6 altos, 13 médios) com arquivo:linha, cenário e plano; checklist vivo (marcar Status ao corrigir)
- `docs/ANALISE_AUDIO_ANEXO_ATENDIMENTO.md` — áudio (nota de voz) e anexos pelo composer WEB: o backend (`/responder-midia`) e a timeline já existem (só o app usava); **`shared/audio.py::converter_para_nota_de_voz`** (PR A, 2026-09-19) converte qualquer áudio (WebM/Opus do Chrome, MP4/AAC do Safari, WAV…) para OGG/Opus mono 48 kHz com PyAV ANTES do `send_audio` — o WhatsApp só reconhece nota de voz nesse formato; OGG/Opus passa intacto (detectado pelo demuxer, não pelo MIME); o banco guarda o convertido (`audio/ogg`); áudio ilegível vira 400 sem row. **Composer web** (PR B, 2026-09-19): `frontend/src/app/atendimento/composer-midia.tsx` (clipe, microfone por TOQUE com `MediaRecorder` no formato que o navegador der, prévia, colar imagem, arrastar) + proxy `POST /api/proxy/midia/[atendimentoId]` (Route Handler — Server Action tem corpo de 1 MB; o multipart é reconstruído para o boundary bater); o estado do anexo mora no drawer e o "Enviar" de sempre manda, com o texto do composer como legenda (uma mensagem só). Nota interna e edição desligam mídia. `accept` sem `image/*` de propósito: no iPhone é o que faz o seletor entregar JPEG em vez de HEIC
- `docs/CUSTO_E_CAPACIDADE.md` — custo Meta (cobrança de service a partir de 1º/10/2026, R$ 0,035/msg), custo de IA medido, capacidade do servidor, VPS e isolamento para expandir a 10 clientes
